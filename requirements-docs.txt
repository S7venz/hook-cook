# Dépendances MkDocs pour le site de documentation Hook & Cook.
# Installation locale : pip install -r requirements-docs.txt
# Servir en local       : mkdocs serve
# Builder le site       : mkdocs build --strict

mkdocs-material>=9.5
mkdocs-glightbox>=0.4
pymdown-extensions>=10.0
